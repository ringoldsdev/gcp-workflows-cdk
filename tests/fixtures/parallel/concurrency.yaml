- work:
    parallel:
      concurrency_limit: 3
      for:
        value: i
        range: [1, 20]
        steps:
          - process:
              call: http.get
              args:
                url: '${"https://example.com/item/" + string(i)}'
