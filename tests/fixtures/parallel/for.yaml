- init:
    assign:
      - total: 0
      - items: [1, 2, 3, 4, 5]
- process:
    parallel:
      shared: [total]
      for:
        value: item
        in: ${items}
        steps:
          - add:
              assign:
                - total: ${total + item}
- done:
    return: ${total}
