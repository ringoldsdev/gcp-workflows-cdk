- check:
    switch:
      - condition: "${x > 0}"
        steps:
          - log_positive:
              call: sys.log
              args:
                text: positive
      - condition: true
        next: fallback
