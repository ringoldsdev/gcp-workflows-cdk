# Call step with result defining a variable
- fetch:
    call: http.get
    args:
      url: "https://example.com/api"
    result: response
- process:
    assign:
      - status: ${response.code}
      - body: ${response.body}
